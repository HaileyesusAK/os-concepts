#ifndef __SORT_H__
#define __SORT_H__

/*
	Sort the integers in 'src' and copy them into 'dst'.
	The elements of 'src' will not remain in the same order.
*/

int sort(int *src, int *dst, size_t size);

#endif
